#!/usr/bin/env bash

# Joins videos

# ./usage:
# videoJoin vid1 vid2 ...

out="${1%.*}"_joined."${1##*.}"
ffmpeg -safe 0 -f concat -i <(for v; do echo file "${PWD// /\\ }/${v// /\\ }"; done) -c copy -strict -2 "${out}"
