# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

# Give the Mod keys appropriate names
set $alt Mod1
set $win Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 9
font pango:Terminus 9
# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Turn off title bar
for_window [class="^.*"] border pixel 2

# Sound (Manipulating sink 1, not sure if that's foolproof)
#bindsym XF86AudioRaiseVolume exec pactl set-sink-volume 1 +1%
#bindsym XF86AudioLowerVolume exec pactl set-sink-volume 1 -- -1%
#bindsym XF86AudioMicMute exec pactl set-sink-mute 1 toggle
bindsym $win+F7 exec pactl set-sink-volume 1 +1%
bindsym $win+F6 exec pactl set-sink-volume 1 -1%
bindsym $win+F5 exec pactl set-sink-mute 1 toggle

# Backlight
#bindsym XF86MonBrightnessDown exec --no-startup-id xbacklight -dec 10
#bindsym XF86MonBrightnessUp exec --no-startup-id xbacklight -inc 10
bindsym $win+F9 exec --no-startup-id xbacklight -dec 5
bindsym $win+F10 exec --no-startup-id xbacklight -inc 5

bindsym XF86Switch_VT_2 exec terminology

# Lock screen
bindsym $win+g exec --no-startup-id i3lock -c 222222

# Suspend
bindsym $win+$alt+g exec --no-startup-id i3lock -c 222222; exec --no-startup-id systemctl suspend

# Screen turns dark after 10 minutes
exec --no-startup-id xset dpms 600

# Screen locks after 12 minutes
#exec --no-startup-id xautolock -time 12 -locker 'i3lock -c 222222'
exec --no-startup-id xautolock -time 12 -locker 'screenlock'

# Use Mouse+$win to drag floating windows to their wanted position
floating_modifier $win

# start a terminal
#bindsym $win+t exec i3-sensible-terminal
bindsym $win+t exec terminology

# kill focused window
bindsym $win+q kill

# start dmenu (a program launcher)
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
bindsym $win+$alt+r exec dmenu_run
bindsym $win+r exec i3-dmenu-desktop

# change focus
bindsym $win+h focus left
bindsym $win+j focus down
bindsym $win+k focus up
bindsym $win+l focus right

# move focused window (5 px is only used for floating windows)
bindsym $alt+h move left 5 px
bindsym $alt+j move down 5 px
bindsym $alt+k move up 5 px
bindsym $alt+l move right 5 px

# split in horizontal orientation
bindsym $win+i split h

# split in vertical orientation
bindsym $win+e split v

# enter fullscreen mode for the focused container
bindsym $win+v fullscreen

# change container layout (stacked, tabbed, toggle split)
#bindsym $win+s layout stacking
bindsym $win+s layout tabbed
#bindsym $win+e layout toggle split

# toggle tiling / floating
bindsym $win+f floating toggle

# change focus between tiling / floating windows
#bindsym $win+space focus mode_toggle

# focus the parent container
bindsym $win+p focus parent

# focus the child container
bindsym $win+c focus child

# switch to workspace
bindsym $win+Right workspace next_on_output
bindsym $win+Left workspace prev_on_output
bindsym $win+1 workspace 1
bindsym $win+2 workspace 2
bindsym $win+3 workspace 3
bindsym $win+4 workspace 4
bindsym $win+5 workspace 5
bindsym $win+6 workspace 6
bindsym $win+7 workspace 7
bindsym $win+8 workspace 8
bindsym $win+9 workspace 9
bindsym $win+0 workspace 10

# move focused container to workspace
bindsym $alt+Right move container to workspace next_on_output; workspace next_on_output
bindsym $alt+Left move container to workspace prev_on_output; workspace prev_on_output
bindsym $alt+1 move container to workspace 1; workspace 1
bindsym $alt+2 move container to workspace 2; workspace 2
bindsym $alt+3 move container to workspace 3; workspace 3
bindsym $alt+4 move container to workspace 4; workspace 4
bindsym $alt+5 move container to workspace 5; workspace 5
bindsym $alt+6 move container to workspace 6; workspace 6
bindsym $alt+7 move container to workspace 7; workspace 7
bindsym $alt+8 move container to workspace 8; workspace 8
bindsym $alt+9 move container to workspace 9; workspace 9
bindsym $alt+0 move container to workspace 10; workspace 10

# move complete workspace to other monitor (toggles when two screens connected)
bindsym $alt+m move workspace to output left

# reload the configuration file
#bindsym $win+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $win+Escape restart
# exit i3 (logs you out of your X session)
bindsym $win+$alt+Escape exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # vim keys are used, though the behavior
        # of vim is only mimicked for windows at the right or bottom
        bindsym h resize grow width 5 px or 5 ppt
        bindsym j resize shrink height 5 px or 5 ppt
        bindsym k resize grow height 5 px or 5 ppt
        bindsym l resize shrink width 5 px or 5 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}
# modes are activated with $alt
bindsym $alt+r mode "resize"

# Make Skype windows floating by default
#for_window [class="Skype"] floating enable

# Fancy colors!
# class                 border  backgr. text    indicator
client.focused          #004400 #007700 #00dddd #000099
client.focused_inactive #002200 #004400 #009999 #000066
#client.unfocused        #333333 #222222 #888888 #292d2e
#client.urgent           #2f343a #900000 #ffffff #900000

# Move back to workspace where you came from by hitting same keys twice
workspace_auto_back_and_forth yes

# Screenshots (using scrot)
bindsym --release Print exec scrot -e 'mv $f ~/Downloads/'
bindsym --release $win+Print exec scrot -s -e 'mv $f ~/Downloads/'

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar{
    status_command i3status
    
    # Show system tray only on primary monitor (as defined by xrandr) [disable
    # for now, seems to be unstable]
    #tray_output primary

    colors{
        background #222222
        # class            border  backgr. text
        #focused_workspace  #082c00 #093600 #8fa8c9
        #inactive_workspace #dddddd #444444 #dddddd
    }
}

# Start skype
exec --no-startup-id skype
# Set displays
exec --no-startup-id setDisplay
# And restart I3 to properly display resolutions and systray
exec i3-msg restart
